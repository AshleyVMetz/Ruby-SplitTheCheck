<%= form_with(model: restaurant, local: true) do |form| %>
  <% if restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(restaurant.errors.count, "error") %> prohibited this restaurant from being saved:</h2>

      <ul>
      <% restaurant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :city %>
    <%= form.text_field :city %>
  </div>

  <div class="field">
    <%= form.label :state %>
    <%= form.text_field :state %>
  </div>

  <div class="field">
     <div class = "radio">
    <h2>Willing to Split the Check?</h2>
    <%= form.radio_button :vote, "split" %>
    <%= form.label :vote, "Split", value: "split" %>
    <%= form.radio_button :vote, "nosplit" %>
    <%= form.label :vote, "No Split", value: "nosplit" %>
    </div>

  </div>


  <div class="actions">
    <%= form.submit "Create Restaurant", class:"button" %>
    <%= link_to "Cancel", :back, class:"button"  %>
   
 
 </div>
<% end %>
